package com.example.transaksi.repository;

import com.example.transaksi.model.TblFee;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblFeeRepository extends JpaRepository<TblFee, Long> {
}
